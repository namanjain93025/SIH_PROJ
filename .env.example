PORT=5000
NODE_ENV=development

MONGO_URI=mongodb://localhost:27017/sih_proj
JWT_SECRET=replace_with_a_long_random_secret

CLIENT_URL=http://localhost:3000

# SMTP (optional in dev — sendEmail logs to console if these are unset)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
